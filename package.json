{
  "name": "cloudflare-status",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare account analytics dashboard for Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}